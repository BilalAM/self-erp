package self.erp.visitorservice;

import org.apache.commons.lang3.StringUtils;
import self.erp.visitorservice.repositories.Visit;

public class VisitorServiceUtils {
    private static boolean validateVisit(Visit visit) {
        return true;
        // if(StringUtils.isE)
    }
}
