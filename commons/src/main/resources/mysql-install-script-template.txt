export DEBIAN_FRONTEND=noninteractive
echo "{sudo -E apt-get -q -y install mysql-server
sudo mysql -u root -p -e "use mysql; ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'mib';"
sudo service mysql stop
sudo service mysql start
mysql -u root -pmib




echo \"" + SUDO_PASSWORD + "\"| sudo -S systemctl start mysqld ";